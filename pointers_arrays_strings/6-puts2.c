#include "main.h"
#include <stdio.h>
/**
 * puts2 - print every other character of a string,
 *         starting with the first character
 * @str: string
*/
void puts2(char *str)
{
	int i;
	int f;

	while (i )
}
