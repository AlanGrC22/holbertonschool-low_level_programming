#include "main.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
/**
 *_atoi - Write a function that convert a string to an integer
 *@s: string
 *Return: Always r
 */
int _atoi(char *s)
{
	int r = 0;

		for (int i = 0; s[i] != '\0'; i++)
		{
		if ()
		r = r * 10 + s[i] - '0';
		}
		while (r = r * 10 + s[i] - '0')

	printf("\n");
	return (r);
}
