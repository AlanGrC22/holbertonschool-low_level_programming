#include "main.h"
/**
 * add - Write a function that adds two integers and returns the result.
 * @one: integer one
 * @two: integer two
 * Return: result
 **/
int add(int one, int two)
{
	return (one + two);
}
